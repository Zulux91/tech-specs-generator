# Node modules: NO subir nunca
node_modules/

# Build: no subir archivos generados de producción
build/

# Logs y sistema operativo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Editor y entorno
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

# VSCode settings, si no quieres compartirlo
.vscode/

# MacOS metadata
*.DS_Store

# Windows metadata
Thumbs.db

# Compilados
*.cache
.cache/

# Dependencias lockfile: si usas yarn o npm, sube solo uno!
# package-lock.json  # (sube este si usas npm)
# yarn.lock          # (sube este si usas yarn)

#Misc
7z
zip
